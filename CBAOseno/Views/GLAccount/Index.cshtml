@using CBAOseno.Core.Models
@model IEnumerable<GLAccount>

@{
    ViewData["Title"] = "GLAccounts";
}


<div class="text-center">
    <h1 class="display-4">List of GLAccounts</h1>
</div>
<div class="text-right">
    
     <a asp-area="" asp-controller="GLAccount" asp-action="Create" class="btn btn-primary">Add new GLAccount</a>
</div>
<br />


@if(Model.Count() > 0)
    {
<table class="table table-bordered table-striped" style='width:100%'>

<thead class="thead-light">
    <tr>
        <th>S/n</th>
        <th>GL Account Name</th>
		<th>Account Balance</th>
		<th>Code</th>
		<th>Main GL Category</th>
        <th>Status</th>
        <th>Options</th>
        
    </tr>
</thead>
<tbody>
    @foreach(var item in Model)
                {
                    <tr>
                        <td>@item.GLAccountId</td>
                        <td>@item.GLAccountName</td>
						<td>@item.GLAccountBalance</td>
						<td>@item.GLAccountCode</td>
						<td>@item.Categories</td>
                        <td>@item.Status</td>
                        <td>
                            <a asp-controller="GLAccount" asp-action="Edit" asp-route-id="@item.GLAccountId" class="btn btn-outline-success">Edit</a>
                           <a asp-controller="GLAccount" asp-action="Detail" asp-route-id="@item.GLAccountId" class="btn btn-outline-primary">Details</a>
                           
                        </td>
                    </tr>
                }
   
</tbody>
</table>
}
    else
    {
        <p>No items created yet</p>
    }

